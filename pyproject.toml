[project]
name = "Polyphonic micro:bit"
version = "2.0.0"
description = "Converts MIDIs to the melody format and allows it to be played back by simulating the micro:bit radio"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.1",
    "pygame>=2.6.1",
]
